CREATE TABLE data (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    time INT(4) NOT NULL,
    file TEXT NOT NULL,
    orig_size INT(4) NOT NULL,
    comp_size INT(4) NOT NULL,
    comp_rate REAL NOT NULL
);


INSERT INTO data (time, file, orig_size, comp_size, comp_rate) VALUES (1574553984, "random.txt", 82, 70, 14.63);
